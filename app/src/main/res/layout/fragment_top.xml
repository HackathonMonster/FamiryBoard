<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:ripple="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/scrollView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/view_height_large">

        <ImageView
            android:id="@+id/top_imageview_mothersday"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:adjustViewBounds="true"
            android:elevation="4dp"
            android:scaleType="fitCenter"
            android:src="@drawable/news_popup" />

        <ImageView
            android:id="@+id/top_imageview_board"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:elevation="4dp"
            android:scaleType="fitCenter"
            android:src="@drawable/img_card_whiteboard"
            android:layout_marginTop="@dimen/element_spacing_large"/>

        <ImageView
            android:id="@+id/top_imageview_album"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:adjustViewBounds="true"
            android:elevation="4dp"
            android:scaleType="fitCenter"
            android:src="@drawable/img_card_album"
            android:layout_marginTop="@dimen/element_spacing_large"/>

        <CalendarView
            android:id="@+id/calendarView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_top_white"
            android:paddingBottom="@dimen/element_spacing_large"
            android:paddingTop="@dimen/element_spacing_large" />


    </LinearLayout>

</ScrollView>

